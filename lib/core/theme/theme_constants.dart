import 'package:flutter/material.dart';

/// The useMaterialThree flag for the Application theme.
const useMaterialThree = true;

/// The colorScheme of the Application.
final colorScheme = ColorScheme.fromSeed(
  seedColor: Colors.deepPurple,
  error: Colors.red,
  onError: Colors.black,
);

/// The labelSmall [TextStyle] of the Application theme.
const labelSmall = TextStyle(
  fontSize: 14,
  fontWeight: FontWeight.w700,
  color: Colors.black,
);

/// The displayLarge [TextStyle] of the Application theme.
const displayLarge = TextStyle(
  fontSize: 18,
  fontWeight: FontWeight.w700,
  color: Colors.black,
);
